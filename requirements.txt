# core library and tests
pytest>=8.0

# HTTP verification service
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
httpx>=0.27

# optional: only needed to serve the tools over MCP (stdio transport).
# The library, the HTTP service and the whole test suite work without it.
# mcp>=1.2
